upstream teenapp_nextjs {
    server teenapp_next_app:3000;
}

server {

    listen 80;

    # Add client_max_body_size to allow larger request sizes
    client_max_body_size 50M;  # Adjust the size limit as needed
    # سرو استاتیک از خروجی collectstatic
    location / {
        proxy_pass         http://teenapp_nextjs;
        proxy_http_version 1.1;
        proxy_set_header   Upgrade $http_upgrade;
        proxy_set_header   Connection "upgrade";
        proxy_set_header   Host $host;
        proxy_set_header   X-Real-IP $remote_addr;
        proxy_set_header   X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
    }
}
